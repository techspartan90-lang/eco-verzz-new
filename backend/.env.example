# Django settings
SECRET_KEY=django-insecure-)9$c8lf442(z8ad!5u!p$mp78#_=pet)91$g+)+p+6e+r5ydjj
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL Configuration
# If these credentials are provided, Django will use PostgreSQL.
# Otherwise, it falls back to SQLite for local development.
DB_NAME=ecoverzz_db
DB_USER=ecoverzz_user
DB_PASSWORD=ecoverzz_password
DB_HOST=localhost
DB_PORT=5432
